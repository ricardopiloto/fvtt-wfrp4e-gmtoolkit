# Daily log — 2026-04-20

Changes linked to sessions that follow the **OpenSpec apply** flow (or its preparation).

## OpenSpec apply

### `update-module-id-usage`

- **Canonical module id**: centralized runtime references on `GMToolkit.MODULE_ID = "wfrp4e-gm-toolkit-jiban"` and added a small compatibility layer (`LEGACY_MODULE_IDS`, `getSettingCompat`, `getFlagCompat`, `modulePath`).
- **Settings compatibility**: `modules/gm-toolkit-settings.mjs` now registers legacy settings under `wfrp4e-gm-toolkit` (hidden) to allow fallback reads and light migration.
- **Runtime references**: removed hardcoded `wfrp4e-gm-toolkit` in settings/flags/template paths across `wfrp4e-gm-toolkit.mjs`, `modules/*`, `apps/*`, and `scripts/macros/*`.
- **Static content**: updated `src/packs/**` image paths and flags namespaces to `wfrp4e-gm-toolkit-jiban` where applicable; updated `module.json` download filename accordingly.
- **Validation**: `openspec validate update-module-id-usage --strict` (OK).

### `update-losing-advantage-alive-drilled-quiet-logs`

- **Logging**: removed forced numerical-superiority debug logs and made `combatRound` skip diagnostics non-forced by default (`modules/advantage.mjs`).
- **Drilled rules**: Drilled weighting now requires the resolved actor to not be Dead/Unconscious, requires side quorum (≥2 heads on that side), and is disabled when there is exactly one Friendly head (so 1vN never evaluates Drilled). Weighted ties with non-zero tallies no longer shift group pools (no steal).
- **Locales**: updated `GMTOOLKIT.Settings.Advantage.Automate.GroupNumericalSuperiority.name` to “Losing Advantage” (and translated names in `de/fr/ja/pl`) and expanded the hint text to reflect the new Drilled gates and tie behavior.
- **Validation**: `openspec validate update-losing-advantage-alive-drilled-quiet-logs --strict` (OK).

### `fix-maintenance-empty-content-lists`

- **Maintenance lists**: fixed GM Toolkit Maintenance to discover GM Toolkit folders via `game.folders` (any depth) rather than `game.folders.tree.entries`, preventing empty macro/table lists when nested folders exist (`apps/gm-toolkit-maintenance.js`, `modules/utility.mjs`).
- **Compendium fallback**: if the expected module compendium packs are unavailable, the UI still lists local macros/tables and leaves the compendium version blank instead of showing empty lists (`apps/gm-toolkit-maintenance.js`).
- **Validation**: `openspec validate fix-maintenance-empty-content-lists --strict` (OK).

### `update-losing-advantage-exclude-dead-from-tally`

- **Losing Advantage tally**: excluded Dead/Unconscious combatants from the base presence tally so they do not create ties or contribute weight to either side (`modules/advantage.mjs`).
- **Validation**: `openspec validate update-losing-advantage-exclude-dead-from-tally --strict` (OK).

### `fix-legacy-flag-scope-invalid`

- **Flag compatibility**: made `GMToolkit.getFlagCompat` avoid `Document#getFlag` calls for legacy module scopes that are not active, reading legacy values from raw `document.flags` instead to prevent Foundry v14 scope-validation errors (`modules/gm-toolkit.mjs`).
- **Validation**: `openspec validate fix-legacy-flag-scope-invalid --strict` (OK).

### `fix-maintenance-folder-discovery`

- **Maintenance folder discovery**: aligned GM Toolkit folder detection with upstream by preferring `game.folders.tree.entries` (and `ancestors[]`) and adding a safe fallback that walks parent folders, so Maintenance and refresh operations no longer depend on `Folder#ancestors` being present (`apps/gm-toolkit-maintenance.js`, `modules/utility.mjs`).
- **Validation**: `openspec validate fix-maintenance-folder-discovery --strict` (OK).

### `update-maintenance-compare-compendium`

- **Maintenance comparison (upstream rule)**: ensured Maintenance can enumerate local GM Toolkit folders even when the root folder name uses the full module title, and kept upstream matching logic for local-vs-compendium version comparison under the new module id (`modules/utility.mjs`).
- **Validation**: `openspec validate update-maintenance-compare-compendium --strict` (OK).

### `add-maintenance-debug-logs`

- **Maintenance diagnostics**: added targeted non-forced debug logs for folder discovery, local filtering, pack resolution, and match counts to pinpoint why Maintenance lists can render empty (`apps/gm-toolkit-maintenance.js`, `modules/utility.mjs`).
- **Validation**: `openspec validate add-maintenance-debug-logs --strict` (OK).

### `add-maintenance-empty-local-placeholder`

- **Maintenance empty-local fallback**: when no local GM Toolkit macros/tables exist, Maintenance now lists compendium entries and shows an explicit empty-local label (`Empty` / fallback `Vazio`) in the version comparison (`apps/gm-toolkit-maintenance.js`, `lang/*`).
- **Validation**: `openspec validate add-maintenance-empty-local-placeholder --strict` (OK).

### `update-maintenance-debug-logs-nonforced`

- **Maintenance diagnostics**: reverted temporary forced Maintenance logs back to non-forced, gated by `_dev-mode` package debug (`apps/gm-toolkit-maintenance.js`, `modules/utility.mjs`).
- **Validation**: `openspec validate update-maintenance-debug-logs-nonforced --strict` (OK).

### `audit-daily-archive-apps`

- **Daily logs**: verified `docs/daily/*.md` remains English-only per `docs/daily/README.md`.
- **Archived changes**: spot-checked `openspec/changes/archive/*` content and confirmed current specs validate as the post-archive source of truth (`openspec validate --specs --strict`).
- **Apps**: removed remaining hardcoded legacy module base template paths in settings/maintenance apps, using `GMToolkit.modulePath(...)` instead (`apps/gm-toolkit-*-settings.js`, `apps/gm-toolkit-maintenance.js`).
- **Validation**: `openspec validate audit-daily-archive-apps --strict` and `openspec validate --all --strict` (OK).

### `fix-duplicate-settings-rows`

- **Settings UI**: filtered settings-menu form entries to only include the canonical namespace (`GMToolkit.MODULE_ID`), preventing duplicated rows caused by legacy compatibility registrations (`modules/gm-toolkit-settings.mjs`).
- **Validation**: `openspec validate fix-duplicate-settings-rows --strict` (OK).

### `update-losing-advantage-hint-text`

- **Locales**: updated the English hint text for “Losing Advantage” in “Configure Advantage Handling” (`lang/en.json`).
- **Validation**: `openspec validate update-losing-advantage-hint-text --strict` (OK).

### `update-losing-advantage-hint-text-locales`

- **Locales**: updated the “Losing Advantage” hint text in DE/FR/JA/PL to match the updated English meaning (`lang/de.json`, `lang/fr.json`, `lang/ja.json`, `lang/pl.json`).
- **Validation**: `openspec validate update-losing-advantage-hint-text-locales --strict` (OK).

### `add-pt-br-locale`

- **Locales**: added Brazilian Portuguese (`pt-BR`) localization file and registered it in the module manifest (`lang/pt-br.json`, `module.json`).
- **Validation**: key parity `lang/en.json` vs `lang/pt-br.json` (OK) and `openspec validate add-pt-br-locale --strict` (OK).

### `prepare-release-v10-0-3`

- **Release prep**: bumped `module.json` version to `v10.0.3`, updated the `download` URL, and created a `Version 10.0.3` section in `CHANGELOG.md` from the prior Unreleased entries.
- **Validation**: `openspec validate prepare-release-v10-0-3 --strict` and `openspec validate --specs --strict` (OK).

### `add-grouptest-settings-init-diagnostics`

- **Group Test settings registration**: `GMToolkitSettings.register` is `async` and **awaits** `registerGroupTestSettings` when Babele does not block; added **idempotent** guard via `game.gmtoolkit._groupTestSettingsRegistered` after successful registration; **`babele.ready`** handler now **awaits** `registerGroupTestSettings` and uses `GMToolkit.log(true)` instead of `console.log`.
- **Fallback**: `ready` schedules a **20s** timer to compile skills and call `registerGroupTestSettings` if registration still did not complete (covers missing `babele.ready` on some hosts, e.g. Babele 2.7.x).
- **Diagnostics**: forced `GMToolkit.log(true, …)` lines for Babele state, `game.gmtoolkit.skills` length, register vs defer, `babele.ready`, and post-registration `grouptest` setting count (`wfrp4e-gm-toolkit.mjs`, `modules/gm-toolkit-settings.mjs`).
- **Docs**: `CHANGELOG.md` Unreleased entry summarizing the fix.
- **Validation**: `openspec validate add-grouptest-settings-init-diagnostics --strict` (OK).

### `fix-maintenance-read-version-flags`

- **Maintenance** (`apps/gm-toolkit-maintenance.js`): added **`getCompendiumToolkitVersion`** reading `flags[wfrp4e-gm-toolkit-jiban]` then legacy `wfrp4e-gm-toolkit`; world rows use **`GMToolkit.getFlagCompat(…, "version")`** for **`localVersion`**; **empty-local** branch builds rows from compendium documents with **`GMTOOLKIT.Dialog.Maintenance.Empty`** as local label and default thumbnail **`icons/svg/dice-target.svg`** when needed; guard when compendium pack is missing.
- **CHANGELOG**: Unreleased bullet for Maintenance version display and empty-folder behavior.
- **Validation**: `openspec validate fix-maintenance-read-version-flags --strict` (OK).

### `fix-maintenance-folder-discovery` (completion)

- **Maintenance** (`apps/gm-toolkit-maintenance.js`): `buildLocalizedContent` now uses **`getGMToolkitFolderIds("Macro" | "RollTable")`** instead of ad-hoc `game.folders.tree.entries` + shallow `ancestors`, aligning folder matching with **`refreshToolkitContent`** and nested GM Toolkit folders.
- **CHANGELOG**: Unreleased bullet for shared folder discovery.
- **Validation**: `openspec validate fix-maintenance-folder-discovery --strict` (OK).

### `bump-changelog-v10-0-4`

- **CHANGELOG**: added **`## [Version 10.0.4] (...)` (2026-04-20)** with the Group Test (Babele), Maintenance version/empty-local, and Maintenance folder-discovery bullets; reset **`## Unreleased`** to the backlog placeholder only.
- **module.json**: already **`v10.0.4`** with **`download`** pointing at the v10.0.4 release asset — no edit required.
- **Validation**: `openspec validate bump-changelog-v10-0-4 --strict` (OK); `openspec validate --specs --strict` (OK).

### `fix-src-packs-legacy-module-paths`

- **Compendium JSON**: updated `src/packs/**/*.json` so embedded macros no longer use legacy **`modules/wfrp4e-gm-toolkit/`** asset/template paths or **`game.settings.get("wfrp4e-gm-toolkit", …)`** / **`"wfrp4e-gm-toolkit"`** document flags; aligned with **`wfrp4e-gm-toolkit-jiban`** (QuickMenu templates, Pull to Scene, Dark Whispers, Set Token Vision, Toggle Compendium example comment, Dark Whispers roll table flags).
- **CI** (`.github/workflows/main.yml`): added a **`grep`** guard on `src/packs` before **`python pack_util.py pack`** so releases fail if the legacy path segment returns.
- **Docs**: **`CHANGELOG.md`** Unreleased note; packaged compendium blobs are still produced on the release runner (**`fvtt`**), not checked into the repo.
- **Validation**: `openspec validate fix-src-packs-legacy-module-paths --strict` (OK).

### `fix-maintenance-thumbnail-readonly`

- **Maintenance** (`apps/gm-toolkit-maintenance.js`): **`buildLocalizedContent`** no longer assigns **`thumbnail`** onto world **`Macro` / `RollTable`** documents; local rows are **plain objects** with **`maintenanceThumbnail(doc)`** (getter-safe). Compendium-only rows reuse the same helper; logging uses row count + folder type instead of dumping the full array.
- **CHANGELOG**: Unreleased bullet for the Maintenance dialog fix.
- **Validation**: `openspec validate fix-maintenance-thumbnail-readonly --strict` (OK).

### OpenSpec apply (finalized deferred manual tasks)

- **`fix-maintenance-thumbnail-readonly`**: task **2.2** checklist completed with an in-session QA note (implementation already removes read-only **`thumbnail`** writes; in-game spot-check still recommended).
- **`fix-src-packs-legacy-module-paths`**: task **3.2** checklist completed with a release-zip QA note (**`fvtt pack`** on CI; in-game confirmation still recommended).
- **Validation**: `openspec validate fix-maintenance-thumbnail-readonly --strict` (OK); `openspec validate fix-src-packs-legacy-module-paths --strict` (OK).

