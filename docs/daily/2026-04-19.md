# Daily log — 2026-04-19

Changes linked to sessions that follow the **OpenSpec apply** flow (or its preparation).

## OpenSpec apply

- Created the `docs/daily/` directory for per-date logs (`yyyy-mm-dd.md`).
- Created this `2026-04-19.md` file as the first entry.
- Updated `.cursor/commands/openspec-apply.md`: explicit step to append to `docs/daily/yyyy-mm-dd.md` when an apply completes.

### `add-drilled-numerical-superiority-weight` + `update-numerical-superiority-include-neutral-enemy` (combined apply)

- **`modules/advantage.mjs`:** `totalDrilledAdvances`, `numericalSuperiorityWeight`; `applyGroupNumericalSuperiority` with weighted totals — **Friendly** → players side; **Hostile** and **Neutral** → enemies side; weight **2** with Drilled (≥1 advance, `NAME.Drilled`), otherwise **1**; no actor → **1** for that disposition; log `playersSide` / `enemySide`.
- **`lang/en.json`**, **`de.json`**, **`fr.json`**, **`ja.json`**, **`pl.json`:** `GroupNumericalSuperiority.hint` updated.
- **`CHANGELOG.md`:** **Unreleased** entry.
- **`openspec/changes/*/specs/group-advantage-automation/spec.md`** and **`tasks.md`:** reconciled; tasks completed.
- **Validation:** `openspec validate add-drilled-numerical-superiority-weight --strict` and `openspec validate update-numerical-superiority-include-neutral-enemy --strict` (OK).

### `fix-drilled-numerical-superiority-encounter-gate`

- **`modules/advantage.mjs`:** `numericalSuperiorityResolvedActor`; first pass sets `encounterHasDrilled` on tallied combatants (Friendly / Hostile / Neutral); weight **2** only when gate is active and resolved actor has Drilled; log includes `encounterHasDrilled` and a sample actor name.
- **`lang/en.json`**, **`de.json`**, **`fr.json`**, **`ja.json`**, **`pl.json`:** `GroupNumericalSuperiority.hint` with Drilled gate wording.
- **`CHANGELOG.md`:** *Fixed* bullet under **Unreleased**.
- **`openspec/changes/fix-drilled-numerical-superiority-encounter-gate/tasks.md`:** completed.
- **Validation:** `openspec validate fix-drilled-numerical-superiority-encounter-gate --strict` (OK).

### `add-drilled-talent-probe-logging`

- **`modules/advantage.mjs`:** tally loop filters **Friendly/Hostile/Neutral** before `numericalSuperiorityWeight`; `totalDrilledAdvances(actor, logContext?)` with `GMToolkit.log(false, …)` (actor, `drilledName`, matches, total; early returns); `{ combatantId, tokenId }` context from loops; `numericalSuperiorityWeight` forwards `logContext`.
- **`CHANGELOG.md`:** *Changed* bullet under **Unreleased**.
- **`openspec/changes/add-drilled-talent-probe-logging/tasks.md`:** completed.
- **Validation:** `openspec validate add-drilled-talent-probe-logging --strict` (OK); `totalDrilledAdvances` only in `advantage.mjs` / superiority path.

### `add-numerical-superiority-drilled-tiebreak`

- **`modules/advantage.mjs`:** `_drilledAdvancesScan` (silent scan, shared with `totalDrilledAdvances`); when `playersSide`/`enemiesSide` tie with sum > 0 → count `playersDrilled` vs `enemiesDrilled` (heads with Drilled ≥1 advance); pool shift as tie-break; tie on drilled heads too → no change; optional `tieBreak` in log.
- **`lang/en.json`**, **`de.json`**, **`fr.json`**, **`ja.json`**, **`pl.json`:** hint for Drilled tie-break.
- **`CHANGELOG.md`:** *Added* bullet under **Unreleased**.
- **`openspec/changes/add-numerical-superiority-drilled-tiebreak/tasks.md`:** completed.
- **Validation:** `openspec validate add-numerical-superiority-drilled-tiebreak --strict` (OK).

### `add-numerical-superiority-entry-trace`

- **`modules/advantage.mjs`:** `GMToolkit.log(true, …)` at entry to `applyGroupNumericalSuperiority` (`numericalSuperiority:enter`, `combatId`, `round`); **`combatRound`** hook with `logNumericalSkip` (reasons: `not_unique_gm` non-forced log, others forced where applicable; `round_below_minimum` when GA + superiority on and `combat.round < 1`); **`combatTurnChange`** message uses `log(false)` and clarifies post-update housekeeping (not Drilled / superiority).
- **`CHANGELOG.md`:** *Changed* bullet under **Unreleased**.
- **`openspec/changes/add-numerical-superiority-entry-trace/tasks.md`:** completed.
- **Validation:** `openspec validate add-numerical-superiority-entry-trace --strict` (OK).

### `update-daily-log-english-only`

- **`docs/daily/README.md`:** English-only policy for this folder.
- **`docs/daily/2026-04-19.md`:** translated to English; heading uses **Daily log — yyyy-mm-dd**.
- **`.cursor/commands/openspec-apply.md`:** daily-log step 6 updated to English and English-only requirement.
- **`CHANGELOG.md`:** *Changed* bullet under **Unreleased** for `docs/daily/` convention.

### `bump-release-10-0-2-changelog`

- **`module.json`:** `"version"` set to **`v10.0.2`**.
- **`CHANGELOG.md`:** new **`## [Version 10.0.2](https://github.com/Jagusti/fvtt-wfrp4e-gmtoolkit/releases/tag/v10.0.2) (2026-04-19)`** with the shipped bullets; **`## Unreleased`** left as backlog/roadmap placeholder only.
- **`openspec/changes/bump-release-10-0-2-changelog/tasks.md`:** checklist updated (optional git tag / GitHub release left unchecked for maintainer).
- **Validation:** `openspec validate bump-release-10-0-2-changelog --strict` (OK).

### `fix-numerical-superiority-drilled-match-visibility`

- **`modules/advantage.mjs`:** **`_normalizeDrilledName`**, **`_talentItemIsDrilled`** (localized `NAME.Drilled` + case-insensitive **Drilled**); **`_drilledAdvancesScan`** uses the same rule; **`applyGroupNumericalSuperiority`** adds forced **`numericalSuperiority:summary`** with **`outcome`** (`zero_tally` / `tied_weight_and_drilled_heads` / `pools_unchanged` / `pools_updated`); removed silent early return on zero tally without summary.
- **`CHANGELOG.md`:** *Fixed* bullet under **Unreleased**.
- **`openspec/changes/fix-numerical-superiority-drilled-match-visibility/tasks.md`:** completed.
- **Validation:** `openspec validate fix-numerical-superiority-drilled-match-visibility --strict` (OK).
