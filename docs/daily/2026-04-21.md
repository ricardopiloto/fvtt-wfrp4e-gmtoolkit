# Daily log — 2026-04-21

## OpenSpec apply / release prep

- **`module.json`**: set **`version`** to **`v10.0.5`** and **`download`** to the **`v10.0.5`** GitHub release asset URL for **`wfrp4e-gm-toolkit-jiban.zip`**.
- **`CHANGELOG.md`**: added **`## [Version 10.0.5] (...)` (2026-04-21)** with the Maintenance thumbnail fix and **`src/packs`** canonical id/path fixes; reset **`## Unreleased`** to the backlog placeholder only.
- **Validation**: manifest JSON parses; no OpenSpec change id attached (metadata-only bump).
